# 2-1.SUFFICIENT-GOLD.sav

**Tower:** 2-1 Tower of Loot
**App version:** v0.7-455
**Base file:** `2-1.sav` (unmodified original, 22 saves)
**Record replaced:** `5-move-test` -> `SUFFICIENT-GOLD`
**All other records byte-identical:** True

## What it tests

Positive control for INSUFFICIENT-GOLD.

Collect the 5-gold bag, then spend exactly that on the first Gold Gate. Same encoding as the failing test, minus the unaffordable third transition.

## Payload

`(floor, x, y)`, 1-based, origin top-left:

```
  1. (3, 5, 9) -> (3, 5, 8)
  2. (3, 5, 8) -> (3, 6, 8)
  current position: (3,6,8)
```

## Expected result

Load succeeds, player ends on the opened Gold Gate tile at (6,8).

## Observed result

_not yet run_

## How to use

1. Back up the real `2-1.sav`.
2. Copy this over it, keeping the name `2-1.sav`.
3. Load the `SUFFICIENT-GOLD` slot.
4. Record the outcome here and in `RESULTS.md`.
5. Restore the backup.
