# 2-1.INSUFFICIENT-GOLD.sav

**Tower:** 2-1 Tower of Loot
**App version:** v0.7-455
**Base file:** `2-1.sav` (unmodified original, 22 saves)
**Record replaced:** `5-move-test` -> `INSUFFICIENT-GOLD`
**Value shape:** {time, data} table (matches the record it replaced)
**All other records byte-identical:** True

## What it tests

Opening a Gold Gate without enough gold.

Collect the 5-gold bag, spend it on the first Gold Gate, then attempt the rightmost Gold Gate with an empty purse.

## Payload

State-changing transitions, `(floor, x, y)`, 1-based, origin top-left:

```
  1. (3, 5, 9) -> (3, 5, 8)
  2. (3, 5, 8) -> (3, 6, 8)
  3. (3,10, 8) -> (3,11, 8)
  current position: (3,11,8)
```

## Expected result

Load failed. The first two transitions are legal, so a pass proves the loader got past them and rejected only the third.

## Observed result

_not yet run_

## How to use

1. Back up the real `2-1.sav` from the game save directory.
2. Copy this file over it, keeping the game-legal name `2-1.sav`.
3. Load the `INSUFFICIENT-GOLD` slot in game.
4. Record the outcome above and in `RESULTS.md`.
5. Restore the backup.
