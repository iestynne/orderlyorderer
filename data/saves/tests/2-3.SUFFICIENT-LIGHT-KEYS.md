# 2-3.SUFFICIENT-LIGHT-KEYS.sav

**Tower:** 2-3 Thieves Guild
**App version:** v0.7-455
**Base file:** `2-3.sav` (unmodified original, 14 saves)
**Record replaced:** `AUTOSAVE_EXIT` -> `SUFFICIENT-LIGHT-KEYS`
**All other records byte-identical:** True

## What it tests

Positive control for INSUFFICIENT-LIGHT-KEYS.

Collect the Light Key at floor 3 (12,6) from the corridor at (12,5), then open the Light Gate at (3,9) from (3,8). Both intermediate legs run along the row-5 and row-7 corridors, which are Empty end to end.

## Payload

`(floor, x, y)`, 1-based, origin top-left:

```
  1. (3,12, 5) -> (3,12, 6)
  2. (3, 3, 8) -> (3, 3, 9)
  current position: (3,3,9)
```

## Expected result

Load succeeds. NOTE: this assumes floor 3 (12,6) is reachable from the tower start without further state changes. If it fails, the cause may be reachability rather than the key logic - check before concluding.

## Observed result

_not yet run_

## How to use

1. Back up the real `2-3.sav`.
2. Copy this over it, keeping the name `2-3.sav`.
3. Load the `SUFFICIENT-LIGHT-KEYS` slot.
4. Record the outcome here and in `RESULTS.md`.
5. Restore the backup.
