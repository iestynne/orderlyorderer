# 2-3.INSUFFICIENT-LIGHT-KEYS.sav

**Tower:** 2-3 Thieves Guild
**App version:** v0.7-455
**Base file:** `2-3.sav` (unmodified original, 14 saves)
**Record replaced:** `AUTOSAVE_EXIT` -> `INSUFFICIENT-LIGHT-KEYS`
**Value shape:** {time, data} table (matches the record it replaced)
**All other records byte-identical:** True

## What it tests

Opening a Light Gate with no Light Keys held.

Step onto the Light Gate at floor 3 (3,9) without collecting any Light Key first.

## Payload

State-changing transitions, `(floor, x, y)`, 1-based, origin top-left:

```
  1. (3, 3, 8) -> (3, 3, 9)
  current position: (3,3,9)
```

## Expected result

Load failed.

## Observed result

_not yet run_

## How to use

1. Back up the real `2-3.sav` from the game save directory.
2. Copy this file over it, keeping the game-legal name `2-3.sav`.
3. Load the `INSUFFICIENT-LIGHT-KEYS` slot in game.
4. Record the outcome above and in `RESULTS.md`.
5. Restore the backup.
