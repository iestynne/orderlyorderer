# 1-5.INSUFFICIENT-POWER.sav

**Tower:** 1-5 Tiny Tower
**App version:** v0.7-455
**Base file:** `1-5.sav` (unmodified original, 35 saves)
**Record replaced:** `moves-test-1` -> `INSUFFICIENT-POWER`
**Value shape:** {time, data} table (matches the record it replaced)
**All other records byte-identical:** True

## What it tests

Attacking an enemy the player cannot defeat.

Move up one space onto the -50 Bat at floor 1 (8,13). The player arrives with insufficient power.

## Payload

State-changing transitions, `(floor, x, y)`, 1-based, origin top-left:

```
  1. (1, 8,14) -> (1, 8,13)
  current position: (1,8,13)
```

## Expected result

Load failed, if validation re-simulates affordability. A successful load means validation is geometric only.

## Observed result

_not yet run_

## How to use

1. Back up the real `1-5.sav` from the game save directory.
2. Copy this file over it, keeping the game-legal name `1-5.sav`.
3. Load the `INSUFFICIENT-POWER` slot in game.
4. Record the outcome above and in `RESULTS.md`.
5. Restore the backup.
